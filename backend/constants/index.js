const MsgType = {
    NOTIFICATION: "notification",
    CHAT_HISTORY: "chatHistory",
    MESSAGE: "message",
    USERS: "users"
}

const ChatType = {
    GLOBAL: "global",
    PRIVATE: "private"
}

module.exports = {
    MsgType,
    ChatType
}